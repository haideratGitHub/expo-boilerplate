const BASE_URL = "/"; //replace with your base url
const TOKEN = "token"; //replace with your jwt token key

const LANGUAGES = {
	defaultLanguage: "en",
	english: "en",
	turkish: "tr",
};

const THEME = {
	dark: "dark",
	light: "light",
};

export { BASE_URL, TOKEN, LANGUAGES, THEME };
